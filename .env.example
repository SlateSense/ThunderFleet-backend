# Server Configuration
PORT=4000

# Server URL for self-pinging (optional - defaults to localhost if not set)
# Use this if your server is deployed and accessible via a public URL
# For your Render deployment, use:
SERVER_URL=https://thunderfleet-backend.onrender.com/health

# External Server URL (optional - for additional health checks)
# If set, the server will ping this URL in addition to self-pinging
# EXTERNAL_SERVER_URL=https://thunderfleet-backend.onrender.com/health

# Speed Wallet Configuration (your existing env vars)
SPEED_WALLET_SECRET_KEY=your_secret_key_here
SPEED_WALLET_WEBHOOK_SECRET=your_webhook_secret_here
